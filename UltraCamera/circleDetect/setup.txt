from setuptools import setup

setup(name='circleDetect',
      version='0.1',
      description='DetectCircles',
      author='IoT SQS',
      license='MIT',
      packages=['circleDetect'],
      install_requires=[
          'numpy',
      ],
      zip_safe=False)
